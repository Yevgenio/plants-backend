stages:
  - build
  - deploy

variables:
  IMAGE_NAME: $CI_REGISTRY_IMAGE
  IMAGE_TAG: $CI_COMMIT_SHORT_SHA
  DOCKER_TLS_CERTDIR: ""
  DOCKER_HOST: tcp://docker:2375

build-and-push:
  stage: build
  image: docker:27
  tags:
    - pipeline
  services:
    - name: docker:27-dind
      command: ["--insecure-registry=10.1.0.11:5050", "--tls=false"]
  script:
    - echo "$CI_REGISTRY_PASSWORD" | docker login $CI_REGISTRY -u "$CI_REGISTRY_USER" --password-stdin
    - docker build -t $IMAGE_NAME:$IMAGE_TAG -t $IMAGE_NAME:latest .
    - docker push $IMAGE_NAME:$IMAGE_TAG
    - docker push $IMAGE_NAME:latest
  after_script:
    - docker logout $CI_REGISTRY
  only:
    - main

deploy:
  stage: deploy
  image: bitnami/kubectl:latest
  tags:
    - pipeline
  script:
    - echo "$KUBE_CONFIG" | base64 -d > /tmp/kubeconfig
    - kubectl --kubeconfig=/tmp/kubeconfig set image deployment/gallery-backend gallery-backend=$IMAGE_NAME:$IMAGE_TAG -n gallery
    - kubectl --kubeconfig=/tmp/kubeconfig rollout status deployment/gallery-backend -n gallery --timeout=120s
  only:
    - main
